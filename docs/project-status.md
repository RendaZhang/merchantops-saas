# Project Status

Last updated: 2026-04-26

> Maintenance note: keep this page as the source of truth for current implementation reality, current public baseline, and known gaps. If the active planning boundary, public endpoints, automated coverage, or current limitations change, update this page before mirroring the change into [roadmap.md](roadmap.md) or [product-strategy.md](product-strategy.md) when applicable.

## Overview

MerchantOps SaaS now sits on a completed Week 1-10 working-tree baseline with Week 10 Delivery Hardening and Portfolio Packaging tagged as the current beta baseline. The public surface covers tenant-scoped user management, one real tenant-scoped feature-flag management API, ticket workflow, audit/approval, import jobs, read-only ticket AI summary, triage, internal reply-draft, entity-scoped interaction-history endpoints, one tenant-scoped AI usage-summary endpoint, four read-only import AI endpoints, and two completed Week 8 human-reviewed execution bridges: approval-bounded import selective replay and approval-bounded ticket comment creation from AI reply-draft output. Week 8 governance hardening now also ships as the shared approval baseline: pending proposals are deduplicated on executable payload semantics, concurrent duplicate creates collapse to one pending row, and resolved requests release their pending key for future proposals. Week 9 is now complete as the executable prompt-inventory and eval-comparator baseline together with the tenant-scoped usage/cost governance read surface and prompt-version aggregate visibility on that same tenant summary. Week 10 Slice A is complete as persisted rollout control for the six AI generation endpoints and the two Week 8 workflow bridges. Week 10 Slice B is complete as a reproducible Dockerized API delivery baseline with repository-root boot-jar packaging, a root multi-stage Dockerfile, a pinned shared infra network, and verified container startup against the existing MySQL, Redis, and RabbitMQ stack. Week 10 Slice C is complete as a minimal GitHub Actions CI quality gate for pull requests and `main` pushes, running the default Maven regression plus Docker image build without secrets, deployment, image publishing, live AI provider checks, Dockerized API live smoke, or the opt-in real MySQL migration suite. Week 10 Slice D is complete as a portfolio and open-source handoff packaging pass: the root README is a concise project entry point, `docs/getting-started/project-showcase.md` gives a practical demo path, and navigation points to that handoff page. Productization Baseline is now active. Slice A introduced the first standalone admin-console frontend entry for login, current tenant context, token restoration, and workflow navigation placeholders; Slice B added server-side auth sessions, required JWT `sid` claims, current-session logout/revocation, and admin-console sign-out; Slice C adds the production-like same-origin admin + API runtime contract with an Nginx-served admin container, runtime compose overlay, runtime profile, explicit secret-injection contract, and CI coverage for admin checks plus API/admin image construction; Slice D adds database-level same-tenant enforcement for `user_role` bindings; Slice E adds the first real read-only admin workflow screen with a protected `/tickets` route over the existing ticket queue API; Slice F adds database-level same-tenant enforcement for root ticket assignee and creator references; Slice G-A adds a status-aware auth-session cleanup scheduler with conservative runtime defaults and no frontend contract change; Slice G-B1 adds current-user logout-all across active auth sessions; and Slice H1 adds a protected `/feature-flags` admin control screen over the existing feature-flag API.

## Current Phase Summary

- Current phase: Productization Baseline active; Slice A Minimal Admin Console Entry, Slice B Server-Side Auth Session + Logout Foundation, Slice C Same-Origin Admin + API Runtime Contract, Slice D Tenant Integrity Hardening, Slice E First Workflow Screen, Slice F Tenant Actor Integrity Follow-Up, Slice G-A Auth Session Cleanup Scheduler, Slice G-B1 Logout-All Sessions Contract, and Slice H1 Feature Flags Control Screen are complete. The next active implementation slice has not been selected yet.
- Current tagged milestone: `v0.7.0-beta`, recorded as `Week 10 complete: Delivery Hardening and Portfolio Packaging beta baseline`.
- Previous tagged milestone: `v0.6.0-beta`, recorded as `Week 9 complete: AI Governance, Eval, Cost, and Usage beta baseline`.
- Stable completed tagged baselines: Week 2 tenant user management, Week 3 ticket workflow, Week 4 audit/approval, Week 5 async import and data operations, Week 6 AI Copilot for Ticket Operations, Week 7 AI Copilot for Import and Data Quality, Week 8 Agentic Workflows with Human Oversight, and Week 9 AI Governance, Eval, Cost, and Usage.
- Week 5 remains complete with import submission/list/detail/error reporting, narrowed `USER_CSV` business-row execution, filtered queue reads, failed-row replay variants, whole-file replay for full-failure sources, and derived-job lineage.
- Week 6 is now complete with four public suggestion-only or read-only ticket AI slices exposed as `GET /api/v1/tickets/{id}/ai-interactions`, `POST /api/v1/tickets/{id}/ai-summary`, `POST /api/v1/tickets/{id}/ai-triage`, and `POST /api/v1/tickets/{id}/ai-reply-draft` under tenant scope, `TICKET_READ`, explicit prompt versioning, controlled provider degradation, dedicated AI interaction persistence, and operator-visible runtime usage/cost metadata on interaction history.
- Week 7 Slice A is now live as `POST /api/v1/import-jobs/{id}/ai-error-summary` under tenant scope and `USER_READ`, with read-only suggestion-only behavior, prompt-time row sanitization, controlled provider degradation, and dedicated `ai_interaction_record` persistence as `entityType=IMPORT_JOB` plus `interactionType=ERROR_SUMMARY`.
- Week 7 Slice B is now live as `POST /api/v1/import-jobs/{id}/ai-mapping-suggestion` under tenant scope and `USER_READ`, reusing the same import AI runtime, sanitized-context rule, and degraded-mode model while staying read-only and suggestion-only.
- Week 7 Slice C is now live as `POST /api/v1/import-jobs/{id}/ai-fix-recommendation` under tenant scope and `USER_READ`, reusing the same import AI runtime while staying read-only, suggestion-only, grounded in local row-level `errorCode` groups, and protected by local sensitive-output rejection.
- Week 7 Slice D is now live as `GET /api/v1/import-jobs/{id}/ai-interactions` under tenant scope and `USER_READ`, reusing existing `ai_interaction_record` storage for narrowed, read-only, operator-visible import AI history with exact-match filters and stable ordering `createdAt DESC, id DESC`.
- Week 8 Slice A is now live as `POST /api/v1/import-jobs/{id}/replay-failures/selective/proposals`, creating `IMPORT_JOB_SELECTIVE_REPLAY` approval requests with a narrow safe payload and approve-time dispatch into the existing selective replay execution path.
- Week 8 Slice B is now live as `POST /api/v1/tickets/{id}/comments/proposals/ai-reply-draft`, creating `TICKET_COMMENT_CREATE` approval requests with a narrow safe payload and approve-time dispatch into the existing ticket comment write chain.
- Week 8 Slice C is now live as shared approval hardening: `pending_request_key` is action-aware across disable, import replay proposal, and ticket comment proposal flows; duplicate pending proposals now return controlled `400` responses; and resolved requests clear the key so the same executable payload can be proposed again later.
- Week 9 Slice A is now complete as the executable AI prompt inventory and eval comparator baseline: the six generation workflows share a main-code workflow catalog, the repository now carries per-workflow golden plus failure plus policy datasets, and the default Maven suite runs a shared comparator pass across all six workflows without changing any public endpoint, migration, or history/billing semantics.
- Week 9 Slice B is now complete as `GET /api/v1/ai-interactions/usage-summary` under tenant scope and `USER_READ`, aggregating current-tenant `ai_interaction_record` rows across ticket and import AI interactions with optional inclusive `from` / `to` plus exact-match trimmed `entityType` / `interactionType` / `status` filters into totals plus `byInteractionType` / `byStatus` breakdowns without exposing raw prompt text, raw provider payload, or billing / ledger semantics.
- Week 9 Slice C is now complete as an additive widening of the same tenant-scoped `GET /api/v1/ai-interactions/usage-summary` surface: the response now also includes stable `byPromptVersion` buckets with `count`, `succeededCount`, `failedCount`, `totalTokens`, and `totalCostMicros`, still under the same filters and still without exposing raw prompt text, raw provider payload, request-level cross-entity detail, or billing / ledger semantics.
- Week 9 is now complete with Slice A plus Slice B plus Slice C as the current eval plus governance visibility baseline on top of the completed Week 8 workflow foundation.
- Week 10 Slice A is now complete as real feature-flag hardening: `GET /api/v1/feature-flags` plus `PUT /api/v1/feature-flags/{key}` now manage one fixed tenant-scoped eight-key inventory per current tenant, list synthesizes default-enabled items for missing known rows, `PUT` requires a concrete boolean `enabled` value and rejects `enabled=null` with controlled `400`, update creates a missing known row from that default inventory baseline when needed, the six AI generation endpoints now require both `merchantops.ai.enabled=true` and their matching persisted AI flag for the current tenant, the two Week 8 workflow proposal bridges now each require their own persisted workflow flag with controlled `503` behavior and no hidden fallback, and concurrent already-applied flag updates now return the final persisted row without emitting duplicate `FEATURE_FLAG_UPDATED` audit rows.
- Week 10 Slice B is now complete as a Dockerized API delivery baseline: the repository root now ships a multi-stage `Dockerfile`, `merchantops-api` now packages a runnable Spring Boot boot jar during reactor builds, `.dockerignore` trims the build context, the infra stack keeps a pinned `merchantops-infra` bridge network while still only running MySQL / Redis / RabbitMQ, and the documented `docker build` plus `docker run --env-file .env --network merchantops-infra ...` path has been verified through `/health`, `/actuator/health`, admin login, and authenticated `GET /api/v1/context`.
- Week 10 Slice C is now complete as a minimal CI/CD baseline: `.github/workflows/ci.yml` runs on pull requests and `main` pushes, uses Temurin JDK 21 with Maven dependency caching for `./mvnw -pl merchantops-api -am test`, and separately verifies `docker build -t merchantops-api:ci .` without deployment, registry publishing, live AI provider secrets, Dockerized API live smoke, or opt-in real MySQL migration execution.
- Week 10 Slice D is now complete as portfolio and open-source handoff packaging: `README.md` now frames the project as a workflow-first, AI-enhanced SaaS reference implementation; `docs/getting-started/project-showcase.md` gives a 5-10 minute demo and handoff path; and docs navigation now surfaces that page for new readers.
- Productization Baseline Slice A is now complete as the minimal admin console entry: `merchantops-admin-web/` is a standalone Vite + React + TypeScript app with React Router, TanStack Query, Tailwind CSS, Zod-validated login/context responses, local JWT restoration, and navigation placeholders for tickets, approvals, imports, AI interactions, and feature flags.
- Productization Baseline Slice B is now complete as the server-side auth-session and logout foundation: `POST /api/v1/auth/login` creates an `ACTIVE` `auth_session`, JWTs carry a required `sid` claim, protected requests validate active session state before tenant/user/role revalidation, `POST /api/v1/auth/logout` revokes only the current session, and the admin console now uses a real `Sign out` flow. Refresh tokens, cookies, and token rotation remained deferred beyond that slice.
- Productization Baseline Slice C is now complete as the same-origin admin + API runtime contract: `merchantops-admin-web` builds into an Nginx static runtime container on `http://localhost:8081`, proxies `/api/...` to the `merchantops-api` container over `merchantops-infra`, keeps the API debug port on `8080`, adds an explicit `runtime` Spring profile, documents runtime-injected secrets/config, and widens CI with admin frontend checks plus API/admin image builds. CORS, cookies, refresh tokens, token rotation, real secret-manager integration, image publishing, K8s, Helm, TLS, and deployment automation remain deferred.
- Productization Baseline Slice D is now complete as tenant-integrity hardening for `user_role`: `V16__enforce_user_role_tenant_integrity.sql` adds `user_role.tenant_id`, backfills it from `users`, adds composite same-tenant foreign keys to both `users` and `role`, and updates role-binding writes plus RBAC reads so user-role joins express the database invariant.
- Productization Baseline Slice E is now complete as the first read-only admin workflow screen: `merchantops-admin-web` adds a shared authenticated layout, protected `/tickets` route, Zod-validated ticket page schema, authenticated `GET /api/v1/tickets?page=0&size=10` API client call, loading/empty/error states, and a read-only current-tenant queue table. Ticket detail, mutations, filters, pagination controls, AI actions, approval actions, backend API changes, refresh tokens, and CORS remain deferred.
- Productization Baseline Slice F is now complete as ticket row actor tenant-integrity hardening: `V17__enforce_ticket_actor_tenant_integrity.sql` adds child indexes and composite same-tenant foreign keys from `ticket(assignee_id, tenant_id)` and `ticket(created_by, tenant_id)` to `users(id, tenant_id)`. `assignee_id` remains nullable, service-layer ticket actor validation is unchanged, and ticket comment authors, operation-log operators, and child-table ticket tenant constraints remain later follow-up work.
- Productization Baseline Slice G-A is now complete as auth-session lifecycle cleanup: shared config now exposes `merchantops.auth.session.cleanup.*`, the scheduled cleanup path deletes at most one batch per run, expired `ACTIVE` rows become eligible only after the retention window measured from `expires_at`, `REVOKED` rows become eligible only after the retention window measured from `revoked_at`, `V18__store_auth_session_times_as_datetime.sql` keeps `auth_session.created_at` / `expires_at` / `revoked_at` on `DATETIME` columns, login plus request-time auth now use UTC instants end to end, and tokens whose old session rows were later deleted still return controlled `401 authentication required`. Refresh tokens, cookies, and token rotation remain deferred.
- Productization Baseline Slice G-B1 is now complete as the logout-all sessions contract: `POST /api/v1/auth/logout-all` revokes every `ACTIVE` auth session for the authenticated current user in the current tenant, including the caller's current session, while preserving other users and other tenants. The admin console now exposes `Sign out all sessions` beside the current-session sign-out action without changing local token storage, JWT claims, cleanup, refresh-token, cookie, rotation, or same-origin runtime boundaries.
- Productization Baseline Slice H1 is now complete as the Feature Flags control screen: `merchantops-admin-web` adds a protected `/feature-flags` route over the existing `GET /api/v1/feature-flags` and `PUT /api/v1/feature-flags/{key}` API, validates the list and update payloads with Zod, renders the fixed current-tenant flag inventory with per-row enable/disable controls, refreshes the flag query after updates, and shows an in-page `权限不足` state for generic permission `403` responses without clearing the local session. Cross-tenant admin, percentage rollout, environment policy, batch editing, audit detail, AI provider configuration, backend API changes, refresh tokens, cookies, and token rotation remain deferred.
- Exact endpoint contracts live in [reference/README.md](reference/README.md); this page keeps the phase-level truth and current limits.

## Release Baseline

- Current tagged milestone: `v0.7.0-beta` on 2026-04-12, recorded as `Week 10 complete: Delivery Hardening and Portfolio Packaging beta baseline`.
- Previous tagged milestone: `v0.6.0-beta` on 2026-04-06, recorded as `Week 9 complete: AI Governance, Eval, Cost, and Usage beta baseline`.
- Earlier previous tagged milestone: `v0.5.0-beta` on 2026-04-04, recorded as `Week 8 complete: Agentic Workflows with Human Oversight beta baseline`.
- Earlier previous tagged milestone: `v0.4.0-beta` on 2026-03-28, recorded as `Week 7 complete: AI Copilot for Import and Data Quality beta baseline`.
- Earlier milestones: `v0.2.0-alpha` on 2026-03-19 (`Week 5 complete: async import and data operations preview`), `v0.1.3` on 2026-03-12 (`Week 4 complete: audit and approval baseline`), `v0.1.2` on 2026-03-11 (`Week 3 complete: ticket workflow baseline`), `v0.1.1` on 2026-03-11 (`Week 2 complete: tenant user management loop`), and `v0.1.0` on 2026-03-09 (`Week 1 complete: foundation phase`).
- The current tagged baseline records the completed Week 10 delivery hardening and portfolio packaging line on top of the completed Week 9 governance, eval, cost, and usage line.

## Current Repository Baseline

### Public workflow surface

- Auth and context: `POST /api/v1/auth/login`, `POST /api/v1/auth/logout`, `POST /api/v1/auth/logout-all`, `GET /api/v1/user/me`, and `GET /api/v1/context`.
- User management: tenant-scoped list/detail/create/update/status/role-assignment plus role lookup and disable-request initiation.
- Feature flags: tenant-scoped `GET /api/v1/feature-flags` and `PUT /api/v1/feature-flags/{key}` under `FEATURE_FLAG_MANAGE`, exposing one fixed eight-key inventory per tenant for AI generation and workflow-bridge rollout control, synthesizing default-enabled items for missing known rows on list, creating missing known rows on update when first changed, requiring a non-null boolean `enabled` value, returning the final persisted row, and skipping duplicate audit rows when a concurrent write already reached the requested state.
- Ticket workflow: tenant-scoped list/detail/create/assignee/status/comment flow with queue filters, reopen support, and a separate ticket comment proposal endpoint for human-reviewed reply-draft execution.
- AI-assisted ticket read path: `GET /api/v1/tickets/{id}/ai-interactions` for narrowed interaction-history visibility plus operator-visible runtime usage/cost metadata, `POST /api/v1/tickets/{id}/ai-summary` for suggestion-only summaries, `POST /api/v1/tickets/{id}/ai-triage` for suggestion-only classification and priority guidance, and `POST /api/v1/tickets/{id}/ai-reply-draft` for internal comment-style reply drafts from ticket detail context.
- AI governance read path: `GET /api/v1/ai-interactions/usage-summary` for tenant-scoped aggregate runtime metadata across stored ticket and import AI rows, with inclusive `from` / `to`, optional exact-match `entityType` / `interactionType` / `status` filters, aggregate token/cost totals, stable `byInteractionType` / `byStatus` / `byPromptVersion` ordering, and no per-request cross-entity detail listing.
- AI-assisted import read path: `GET /api/v1/import-jobs/{id}/ai-interactions` for narrowed stored import AI interaction history with operator-visible runtime usage/cost metadata when present; `POST /api/v1/import-jobs/{id}/ai-error-summary` for a suggestion-only summary built from current-tenant import detail, `errorCodeCounts`, and the first sanitized failed-row window without forwarding raw CSV payload values to the provider; `POST /api/v1/import-jobs/{id}/ai-mapping-suggestion` for a suggestion-only canonical-field mapping proposal built from sanitized header/global parse signal plus the same bounded row-summary context; and `POST /api/v1/import-jobs/{id}/ai-fix-recommendation` for a suggestion-only fix recommendation built from grounded row-level `errorCode` groups without returning replacement values.
- Governance: entity-scoped `GET /api/v1/audit-events` plus an action-aware approval request queue/detail/approve/reject flow for `USER_STATUS_DISABLE`, `IMPORT_JOB_SELECTIVE_REPLAY`, and `TICKET_COMMENT_CREATE`, with shared pending-request-key deduplication on executable payload semantics and key release after approval resolution.
- Import jobs: tenant-scoped create/list/detail/ai-interactions/ai-error-summary/ai-mapping-suggestion/ai-fix-recommendation/replay/replay-file/selective-replay/selective-replay-proposal/edited-replay/error-page flow with `USER_CSV` processing, filtered queue reads, quoted CSV record parsing, `errorCodeCounts`, row-level item errors, replay-derived job lineage, scope-only replay audit metadata, and the current Week 5 plus Week 7 plus Week 8 reporting/guidance surface.

### Shared runtime and internal baseline

- Multi-module Spring Boot backend with MySQL, Redis, RabbitMQ, Flyway, request tracing, and unified API response / exception handling.
- Standalone `merchantops-admin-web/` frontend with a local-only Vite dev proxy to `http://localhost:8080`, an Nginx production-like runtime on `http://localhost:8081` that proxies same-origin `/api/...` calls to the API container, an API client boundary for `POST /api/v1/auth/login`, `GET /api/v1/context`, `POST /api/v1/auth/logout`, `POST /api/v1/auth/logout-all`, `GET /api/v1/tickets?page=0&size=10`, `GET /api/v1/feature-flags`, and `PUT /api/v1/feature-flags/{key}`, protected Dashboard, Tickets, and Feature Flags routes, and a local token boundary under `merchantops.admin.auth.v1`.
- Tenant-aware query and command services for users, tickets, approvals, import jobs, and the current AI read paths including ticket interaction history, tenant AI usage summary, ticket summary, ticket triage, and ticket reply draft.
- JWT claim revalidation against active server-side auth session state, current tenant status, user status, roles, and permissions on protected requests. Sidless beta tokens, revoked sessions, expired sessions, and mismatched sessions now return controlled `401` responses; inactive tenants, inactive users, and stale role/permission claims remain controlled `403` responses. Current-session logout revokes only one `sid`, logout-all bulk-revokes all active sessions for the current tenant/user, and the background cleanup scheduler deletes expired `ACTIVE` sessions plus retention-aged `REVOKED` sessions without changing request-time auth semantics.
- Role bindings now carry `user_role.tenant_id` and are protected by database-level same-tenant composite foreign keys to both `users(id, tenant_id)` and `role(id, tenant_id)`; role and permission reads join through the same tenant boundary.
- Root ticket actor fields now have database-level same-tenant composite foreign keys for `ticket(assignee_id, tenant_id)` and `ticket(created_by, tenant_id)` to `users(id, tenant_id)`; unassigned tickets remain valid because `assignee_id` stays nullable.
- Instance-level AI provider configuration under `merchantops.ai.*`, repository-root `.env` bootstrap for local dev-profile `spring-boot:run`, explicit summary, triage, reply-draft, and import-error-summary prompt versions, timeout-based degradation, persisted `feature_flag` rollout control, and provider-normalized adapters that now support OpenAI `RAW_HTTP` Responses, OpenAI `SPRING_AI` chat-completions transport, and DeepSeek Chat Completions JSON-output paths for the current ticket and import AI slices.
- A root multi-stage Dockerfile now builds the runnable `merchantops-api` boot jar from the repository-root Maven reactor and runs it as a non-root process on Temurin 21 JRE, while `docker-compose.runtime.yml` joins the pinned `merchantops-infra` network, reaches MySQL / Redis / RabbitMQ by service name, persists import files under `api_import_data`, and serves the built admin app through Nginx.
- `.github/workflows/ci.yml` now provides the no-secret GitHub Actions quality gate for PR and `main` push validation, with independent Maven regression, admin frontend check, and Docker image build jobs for both `merchantops-api` and `merchantops-admin-web`.
- The open-source handoff surface now includes a high-level root README plus a getting-started project showcase page for reviewers who need project purpose, run path, demo order, governance boundaries, CI proof, and current limitations without reading the full runbooks first.
- Dedicated `ai_interaction_record` persistence for AI runtime metadata, separate from `ticket_operation_log` and generic `audit_event`, with current public rows for both `TICKET` and `IMPORT_JOB` entities.
- Local import file storage abstraction with after-commit queue publish, scheduled queued/stale-processing recovery, worker consumption, stale-processing restart/fail handling, system-generated replay-file writes, and configurable chunk / recovery / row-limit controls.
- Focused automated coverage for auth, user management, ticket workflow, AI interaction history, AI summary, AI triage, AI reply draft, import jobs, audit, and approval behavior, including Week 8 duplicate-proposal suppression, reproposal-after-resolution, concurrent duplicate-create collapse, approval-request migration hardening, and an opt-in real MySQL Flyway path for the `V12 -> V13` fresh-db migration chain.

## Current Public AI Baseline

- The current public AI slices are `GET /api/v1/tickets/{id}/ai-interactions`, `POST /api/v1/tickets/{id}/ai-summary`, `POST /api/v1/tickets/{id}/ai-triage`, `POST /api/v1/tickets/{id}/ai-reply-draft`, `GET /api/v1/ai-interactions/usage-summary`, `GET /api/v1/import-jobs/{id}/ai-interactions`, `POST /api/v1/import-jobs/{id}/ai-error-summary`, `POST /api/v1/import-jobs/{id}/ai-mapping-suggestion`, and `POST /api/v1/import-jobs/{id}/ai-fix-recommendation`.
- All nine slices are read-only. The six `POST` endpoints remain suggestion-only: they do not change ticket status, write comments, mutate import jobs, mutate import error rows, or trigger approvals or replay flows, and the three `GET` endpoints only expose narrowed stored metadata or aggregate runtime metadata.
- The six generation endpoints now require both config-level `merchantops.ai.enabled=true` and their matching persisted feature flag, while `GET /api/v1/tickets/{id}/ai-interactions`, `GET /api/v1/import-jobs/{id}/ai-interactions`, and `GET /api/v1/ai-interactions/usage-summary` remain available when generation is gated off.
- Reply-draft semantics are explicitly internal and comment-style: the endpoint returns a structured draft plus `draftText`, but still does not create a comment or send an external message.
- The current AI context is limited to the target current-tenant ticket's core fields plus the most recent comments and workflow logs, with prompt-time truncation and explicit `earlier ... omitted` markers when older history exists.
- The import AI context is limited to current-tenant import detail, `errorCodeCounts`, sanitized header/global parse-error signal, grounded row-level `errorCode` groups, and the first 20 failed rows after local structural sanitization; raw `itemErrors.rawPayload` values, including `USER_CSV.password`, are not forwarded to the provider, and fix recommendation additionally rejects sensitive echoed output before returning a response.
- The interaction-history surfaces return both successful and controlled-failure records with exact-match `interactionType` and `status` filters plus stable `createdAt DESC, id DESC` ordering.
- The interaction-history surfaces now expose ticket-scoped and import-scoped runtime usage/cost metadata when present, return those fields as `null` when unavailable, and still do not expose raw prompt text or raw provider payload.
- The tenant-scoped usage-summary surface aggregates current-tenant `ai_interaction_record` rows across `TICKET` and `IMPORT_JOB`, supports optional inclusive `from` / `to` plus exact-match trimmed `entityType` / `interactionType` / `status` filters, returns totals plus `byInteractionType` / `byStatus` / `byPromptVersion` breakdowns, and intentionally excludes per-request fields such as `requestId`, `outputSummary`, and `modelId` while exposing `promptVersion` only as aggregate prompt-version buckets.
- The current provider ownership model is instance-level configuration rather than tenant BYOK.
- The current Week 9 governance baseline now includes an executable six-workflow inventory, one shared comparator pass for golden, failure, and policy datasets, and one tenant-scoped usage-summary read surface over stored runtime metadata with prompt-version aggregate visibility; broader reporting still comes after that narrow baseline rather than before it.
- The completed Week 6 ticket AI baseline and the completed Week 7 import AI read baseline stay stable while the completed Week 8 workflow now adds two separate approval-backed execution bridges: import selective replay proposals and ticket comment proposals. The nine public AI endpoints themselves still do not write back or self-execute, and the two workflow bridges are now separately controllable through persisted workflow flags.

## Current Limitations

- Import jobs currently support one business import type only: `USER_CSV`.
- The `USER_CSV` schema is fixed to `username,displayName,email,password,roleCodes`.
- Approval flow currently covers three action types only: `USER_STATUS_DISABLE`, `IMPORT_JOB_SELECTIVE_REPLAY`, and `TICKET_COMMENT_CREATE`, but pending uniqueness now spans all three rather than only disable.
- Audit reads are still minimal and entity-scoped by `entityType + entityId`, and the new tenant AI usage-summary endpoint is still only a narrow aggregate governance read rather than a general reporting system.
- `UserCommandService#updatePassword` remains a placeholder business error, not a completed write flow.
- There is no refresh-token flow, cookie/session rotation, session list, device metadata, selective device logout, or logout-all-except-current flow yet. Current-session logout, current-user logout-all, revocation, and background cleanup are implemented through `auth_session`.
- Public AI now includes ticket interaction history, one tenant usage-summary surface, ticket summary/triage/internal reply draft, and import AI interaction history plus error summary plus mapping suggestion plus fix recommendation; the human-reviewed write bridges remain separate workflow endpoints rather than direct AI endpoint mutations.
- The AI read surfaces expose runtime usage/cost metadata only through entity-scoped interaction history and the tenant-scoped usage summary; they are not tenant billing, ledger, or invoice surfaces.
- AI runtime still uses instance-level provider configuration only; there is no tenant BYOK, streaming, tool calling, model routing, or automatic write-back loop.
- Feature flags are intentionally tenant-scoped and fixed-key only in this slice; there is no cross-tenant admin surface, percentage rollout, multi-environment rollout policy, or generic flag platform yet.
- The admin console currently includes the login/context/sign-out/sign-out-all Dashboard, the read-only Tickets queue, and the Feature Flags control screen. Ticket detail, ticket mutations, filters, pagination controls, Approvals, Imports, and AI Interactions remain placeholders or later workflow screens.
- Ticket enrichments such as priority, SLA, attachments, and notifications remain post-Week-3 follow-up work.
- A production-like same-origin admin/API runtime path and runtime secret-injection contract are now supported, but real secret-manager integration, K8s / Helm manifests, TLS/domain management, performance artifacts, deployment automation, and Docker image publishing are still pending.

## Known Gaps And Verification Notes

- Root ticket assignee and creator tenant consistency is now enforced at the database layer. Ticket comment creators, ticket operation-log operators, and child-table ticket tenant relationships remain service-layer or structural follow-ups.
- RBAC endpoints under `/api/v1/rbac/**` are still demo-oriented rather than production-oriented business APIs.
- Focused automated coverage now includes the real feature-flag list/update API, the public AI interaction-history, tenant AI usage-summary, AI summary, AI triage, AI reply-draft, import AI interaction-history plus error-summary plus mapping-suggestion plus fix-recommendation slices, the Week 8 import selective replay proposal/approval flow, the Week 8 ticket comment proposal/approval flow, and the Week 8 Slice C approval hardening baseline: happy path, permission failure, active auth-session creation, UTC-stable JWT/session expiry alignment, required JWT `sid` parsing, current-session logout revocation, logout-all same-user token invalidation with other-user and other-tenant preservation, revoked-session rejection, sidless token rejection, manually expired session rejection, status-aware auth-session cleanup of old `ACTIVE` and `REVOKED` rows, cleanup batch-size enforcement, recent-revoked-session retention, old-token `401` after cleanup, independent multi-session behavior, JVM-timezone-stable JWT claim generation, `V15 -> V18` auth-session migration coverage, stale-claim re-login, database-level rejection of cross-tenant `user_role` bindings, database-level rejection of cross-tenant root ticket assignee/creator bindings, unknown-flag `404`, `enabled=null` validation, idempotent feature-flag no-op updates, concurrent already-applied feature-flag updates that return the final persisted row without duplicate audit, audit snapshots for feature-flag changes, tenant-scoped feature-flag isolation, cross-tenant not-found behavior, inclusive `from/to` filtering, exact-match trimmed usage-summary `entityType` / `interactionType` / `status` filters, history `interactionType` and `status` filters, stable `createdAt DESC, id DESC` ordering, stable usage-summary `byInteractionType` / `byStatus` / `byPromptVersion` ordering, aggregate totals math including null metering rows, history and usage-summary non-leakage assertions, import read-after-write visibility after real generation calls, symmetrical degraded-mode coverage for config-disabled and persisted-flag-disabled gates plus provider-not-configured, provider-unavailable, timeout, and invalid-response paths, provider request-contract assertions, OpenAI `RAW_HTTP` plus OpenAI `SPRING_AI` plus DeepSeek runtime-selector coverage, full `output[].content[]` scanning with multi-part `output_text` parsing where applicable, endpoint-specific required-field validation including import-array blank-item rejection and grounded `errorCode` checks, proposal payload safety, proposal `400` checks for duplicate pending import replay proposals on canonical `errorCodes`, duplicate pending ticket comment proposals on trimmed `commentContent`, invalid ticket or import `sourceInteractionId`, self-approval rejection, repeated review rejection once resolved, approve-time selective replay execution, approve-time single comment creation, reject-without-execution behavior, reproposal after approval or rejection, near-concurrent duplicate-create collapse to one surviving `PENDING` row, no cross-flag leakage, real-provider-path golden-sample regression checks, one shared Week 9 comparator pass across six generation workflows plus golden, failure, and policy datasets, AI-context-window guards, import prompt sanitization checks against raw `USER_CSV` values, import `400` eligibility checks for no-failure, no-header-signal, unsupported-import-type, and no-row-signal jobs, sensitive-output rejection for fix recommendation, and no-business-side-effect assertions across all public AI endpoints.
- Fresh default regression on 2026-04-23 passed with `.\mvnw.cmd -pl merchantops-api -am test`, `BUILD SUCCESS`, and the `merchantops-api` module summary `Tests run: 427, Failures: 0, Errors: 0, Skipped: 1`.
- Focused Productization Baseline Slice F ticket workflow regression on 2026-04-19 passed with `.\mvnw.cmd -pl merchantops-api -am -Dtest=TicketWorkflowIntegrationTest "-Dsurefire.failIfNoSpecifiedTests=false" test` and `Tests run: 31, Failures: 0, Errors: 0, Skipped: 0`.
- Focused Productization Baseline Slice G-B1 auth/RBAC regression on 2026-04-23 passed with `.\mvnw.cmd -pl merchantops-api -am -Dtest=AuthSecurityIntegrationTest "-Dsurefire.failIfNoSpecifiedTests=false" test` and `Tests run: 43, Failures: 0, Errors: 0, Skipped: 0`.
- Productization Baseline Slice H1 frontend workspace validation on 2026-04-26 passed with `npm run typecheck`, `npm run lint`, and `npm run build` from `merchantops-admin-web`.
- Productization Baseline frontend workspace validation on 2026-04-23 passed with `npm run typecheck`, `npm run lint`, and `npm run build` from `merchantops-admin-web`.
- Productization Baseline Slice A local validation on 2026-04-14 passed with browser smoke against the Vite dev proxy plus a temporary API stub, confirming login, local token persistence, dashboard context rendering, workflow navigation placeholders, desktop login image loading, and mobile layout without horizontal overflow.
- Productization Baseline Slice A live local smoke on 2026-04-14 passed with Docker compose infra healthy, Spring Boot `/health` and `/actuator/health` both `UP`, browser login through the Vite dev proxy using `demo-shop` / `admin` / `123456`, real backend `POST /api/v1/auth/login` and `GET /api/v1/context` responses at `200`, dashboard context rendering, refresh-time token restoration, and local session clearing returning to login.
- Productization Baseline Slice B live local smoke on 2026-04-15 passed with Docker compose infra healthy, Spring Boot applying Flyway `V15__add_auth_session.sql`, `/health` and `/actuator/health` both `UP`, Vite dev entry returning `200`, login through the Vite dev proxy using `demo-shop` / `admin` / `123456`, real backend `GET /api/v1/context` returning tenant/user context, `POST /api/v1/auth/logout` returning `SUCCESS` with `data:null`, and the same token returning `401` on `/api/v1/context` after logout.
- Productization Baseline Slice C local image and runtime smoke on 2026-04-16 passed with `docker build -t merchantops-api:local .`, `docker build -t merchantops-admin-web:local .\merchantops-admin-web`, compose startup through `docker-compose.yml` plus `docker-compose.runtime.yml`, API `/health` and `/actuator/health` both `UP`, admin runtime `http://localhost:8081/` returning `200`, SPA history fallback returning `200`, same-origin `POST /api/v1/auth/login` plus `GET /api/v1/context` through `http://localhost:8081/api/...`, `POST /api/v1/auth/logout` returning `SUCCESS` with `data:null`, and the same token returning controlled `401` on `GET /api/v1/context` after logout.
- Productization Baseline Slice D runtime migration smoke on 2026-04-17 passed with `docker compose -f docker-compose.yml -f docker-compose.runtime.yml up -d --build`, Flyway validating 16 migrations and applying `V16__enforce_user_role_tenant_integrity.sql` on MySQL, API `/health` and `/actuator/health` both returning `200`, admin runtime `http://localhost:8081/` returning `200`, same-origin `POST /api/v1/auth/login` plus `GET /api/v1/context` through `http://localhost:8081/api/...`, `POST /api/v1/auth/logout` returning `SUCCESS` with `data:null`, and the same token returning controlled `401` on `GET /api/v1/context` after logout.
- Productization Baseline Slice E local Vite dev-proxy smoke on 2026-04-18 passed with `http://localhost:5173/` and `/tickets` returning `200`, same-origin proxy calls through `http://localhost:5173/api/...` for `POST /api/v1/auth/login`, `GET /api/v1/context`, and `GET /api/v1/tickets?page=0&size=10`, admin/ops/viewer all loading the current tenant ticket queue, `POST /api/v1/auth/logout` returning `SUCCESS` with `data:null`, and the same token returning controlled `401` on `GET /api/v1/context` after logout.
- Productization Baseline Slice E same-origin runtime smoke on 2026-04-18 passed with `docker compose -f docker-compose.yml -f docker-compose.runtime.yml up -d --build`, API `/health` and `/actuator/health` both returning `UP`, admin runtime `http://localhost:8081/` and `/tickets` returning `200`, same-origin calls through `http://localhost:8081/api/...` for `POST /api/v1/auth/login`, `GET /api/v1/context`, and `GET /api/v1/tickets?page=0&size=10`, admin/ops/viewer all loading the current tenant ticket queue, `POST /api/v1/auth/logout` returning `SUCCESS` with `data:null`, and the same token returning controlled `401` on `GET /api/v1/context` after logout.
- Productization Baseline Slice F runtime migration smoke on 2026-04-19 passed with `docker compose -f docker-compose.yml -f docker-compose.runtime.yml up -d --build`, Flyway validating 17 migrations and applying `V17__enforce_ticket_actor_tenant_integrity.sql` on MySQL, API `/health` and `/actuator/health` both returning `UP`, admin runtime `http://localhost:8081/` returning `200`, same-origin login plus `GET /api/v1/context` through `http://localhost:8081/api/...` succeeding for `demo-shop` / `admin`, `POST /api/v1/auth/logout` returning `SUCCESS`, and the same token returning controlled `401` on `GET /api/v1/context` after logout.
- Productization Baseline Slice G-A same-origin runtime smoke on 2026-04-22 passed with `docker compose -f docker-compose.yml -f docker-compose.runtime.yml up -d --build`, Flyway validating 17 migrations with no new schema change required, API `/health` and `/actuator/health` both returning `UP`, admin runtime `http://localhost:8081/` and `/tickets` returning `200`, same-origin `POST /api/v1/auth/login`, `GET /api/v1/context`, and `GET /api/v1/tickets?page=0&size=10` all succeeding for `demo-shop` / `admin`, `POST /api/v1/auth/logout` returning `SUCCESS`, and the same token returning controlled `401` on `GET /api/v1/context` after logout.
- Week 10 Slice B delivery verification on 2026-04-10 succeeded with `docker build -t merchantops-api:local .`, compose-managed infra on `merchantops-infra`, container `/health` plus `/actuator/health`, and admin login plus authenticated `GET /api/v1/context`.
- Week 10 Slice C local validation on 2026-04-11 succeeded with `.\mvnw.cmd -pl merchantops-api -am test` and `docker build -t merchantops-api:ci .`.
- Remote GitHub Actions validation was observed for release-readiness evidence commit `031cb8a60247f6cf8baf7b8be3267162c7bc5fa6`: run [24297294444](https://github.com/RendaZhang/merchantops-saas/actions/runs/24297294444) completed successfully with `Maven Test: success` and `Docker Build: success`, and duplicate run [24297294873](https://github.com/RendaZhang/merchantops-saas/actions/runs/24297294873) also passed for the same commit. If exact release-cut commit CI evidence is required, observe that commit's own Actions run after pushing it.
- The CI quality gate remains no-secret and does not run deployment, image publishing, live AI provider checks, runtime-container smoke, browser smoke, or opt-in real MySQL migration execution. Productization Baseline Slice C widens the original Week 10 CI baseline with admin workspace checks and admin image construction.
- Manual DeepSeek live verification on 2026-04-10 also succeeded on the Dockerized API path with explicit container AI env injection: one `ai.ticket.summary.enabled=false` gate check returned controlled `503` and persisted `FEATURE_DISABLED`, one summary plus triage plus reply-draft sequence returned `deepseek-chat` responses and matching interaction-history rows, `GET /api/v1/ai-interactions/usage-summary` moved by `+4` interactions (`+3 SUCCEEDED`, `+1 FEATURE_DISABLED`) with zero cost fields and positive token totals, `workflow.ticket.comment-proposal.enabled=false` returned controlled `503`, the duplicate pending ticket-comment proposal path now rejects sequential repeats without emitting database uniqueness noise in container logs, and `PUT /api/v1/feature-flags/{key}` still rejects `enabled=null` with controlled `400`.
- Swagger rendering, import AI live paths, real infra health outside the validated ticket path, and modules outside the current focused path still need manual verification.
- Use [runbooks/automated-tests.md](runbooks/automated-tests.md), [runbooks/regression-checklist.md](runbooks/regression-checklist.md), and [runbooks/ai-regression-checklist.md](runbooks/ai-regression-checklist.md) for the current verification baseline.
- Use [architecture/non-blocking-backlog.md](architecture/non-blocking-backlog.md) for tracked non-blocking follow-up items that should not be lost between phases.
