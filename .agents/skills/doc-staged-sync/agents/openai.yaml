interface:
  display_name: "Doc Staged Sync"
  short_description: "Route staged docs or repo-wide doc maintenance"
  default_prompt: "Use $doc-staged-sync to inspect staged changes, the most recent commit, or a no-diff repo-wide maintenance pass and update the required docs."
