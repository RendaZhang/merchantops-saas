interface:
  display_name: "Import Surface Sync"
  short_description: "Sync import workflow and import AI docs"
  default_prompt: "Use $import-surface-sync to align import workflow docs, import AI docs, runbooks, examples, and milestone docs with the current implementation."
