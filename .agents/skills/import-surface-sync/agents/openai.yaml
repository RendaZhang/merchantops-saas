interface:
  display_name: "Import Surface Sync"
  short_description: "Sync import workflow and import AI docs"
  default_prompt: "Use $import-surface-sync to align the current public import workflow docs, approval-backed import execution bridge docs, import AI docs, runbooks, examples, and milestone docs with the current public import surface."
